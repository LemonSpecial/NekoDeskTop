<Page x:Class="NekoDeskTop.SettingPages.General"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:NekoDeskTop.SettingPages"
      Title="General" Height="Auto" Width="Auto">
    <Grid>
        <ScrollViewer>
            <ItemsControl ItemsSource="{Binding ItemGroups}">
                <ItemsControl.Resources>
                    <DataTemplate DataType="{x:Type local:SelectableItem}">
                        <CheckBox Content="{Binding Name}" 
                                  IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                  IsEnabled="{Binding IsEnabled}"
                                  Margin="0,2"/>
                    </DataTemplate>

                    <DataTemplate DataType="{x:Type local:InputItem}">
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <TextBlock Text="{Binding Label}" Width="150" VerticalAlignment="Center"/>
                            <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" 
                                     Width="200"
                                     IsEnabled="{Binding IsEnabled}"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.Resources>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#FFDDDDDD" BorderThickness="0,0,0,1" Margin="0,0,0,10">
                            <StackPanel>
                                <Label Content="{Binding GroupName}" 
                                       FontWeight="Bold"
                                       Margin="0,5"/>
                                <ItemsControl ItemsSource="{Binding Items}" 
                                             Margin="20,0,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>