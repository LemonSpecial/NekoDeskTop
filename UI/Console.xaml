<Window x:Class="NekoDeskTop.UI.Console"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Console" Height="450" Width="800" AllowsTransparency="True">
    <Border BorderBrush="#FF9C9C9C" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0">
                <Label Content="Settings" 
                   FontSize="15" 
                   VerticalAlignment="Center"
                   DockPanel.Dock="Left"
                   Margin="10,0"/>

                <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Right"
                        DockPanel.Dock="Right">
                    <Button Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" 
                        Width="45" Height="30" Content="─" 
                        Click="MinimizeButton_Click"/>
                    <Button Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" 
                        Width="45" Height="30" Content="□" 
                        Click="MaximizeButton_Click"/>
                    <Button Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" 
                        Width="45" Height="30" Content="✕" 
                        Click="CloseButton_Click" />
                </StackPanel>

                <Border Background="Transparent" MouseLeftButtonDown="DragWindow"/>
            </DockPanel>

            <Grid Grid.Row="1">
                <RichTextBox x:Name="log" Background="#FF323232" Foreground="White" 
                        VerticalScrollBarVisibility="Auto" FontFamily="Consolas">
                </RichTextBox>
            </Grid>
        </Grid>
    </Border>
</Window>